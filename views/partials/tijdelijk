  <!--Jouw Facorieten-->
    <section class="yourFavourites">

            <h2>Voor Jou</h2>
            <button id="yourFavouritesDetails">Details +</button>

        <ul class="bookCarousel">
            <% apiItems.forEach(function(apiItem) { %>
                <li>
                    <img class="bookCarouselImg" src='https://fdnd-agency.directus.app/assets/<%= apiItem.afbeelding %>'
                         alt="<%= apiItem.title %> width=" 300" height="500">
                </li>
            <% }) %>
        </ul>
    </section>

    <!--Geleende Boeken-->
    <section class="borrowedBooks">
        <h2>Voor Jou</h2>
        <button id="borrowedBooksDetails">Details +</button>
        <ul class="bookCarousel">
            <% apiItems.forEach(function(apiItem) { %>
                <li>
                    <img class="bookCarouselImg" src='https://fdnd-agency.directus.app/assets/<%= apiItem.afbeelding %>'
                         alt="<%= apiItem.title %> width=" 300" height="500">
                </li>
            <% }) %>
        </ul>
    </section>